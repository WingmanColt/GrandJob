@model ListModel

  <!-- apply modal -->
<div class="modal" id="delete-post-modal">
    <!-- Login Form -->
    <div class="login-form default-form">
      <div class="form-inner">
        <h4>Сигурен ли сте, че искате да изтриете този елемент ?</h4>
        <br>
        @*asp-route-id="@Model.Model.Id" asp-page-handler="@Model.Model.Handler" asp-route-currentPage="@Model.Model.CurrentPage" asp-route-Sort="@Model.Model.Sort"*@
        <!--Login Form-->
        <form method="post" asp-page-handler="@Model.Model.Handler" id="deleteForm">
             <div asp-validation-summary="All" class="text-danger"></div>

          <div class="form-group">
            <label>Напишете заглавието на обявата</label>
            <input asp-for="@Model.Model.Password" type="text" class="form-control" required>
          </div>


        <div class="bottom-box">
          <div class="btn-box row">
                   <div class="col-lg-12 col-md-12">
                   <button class="theme-btn btn-style-danger next-btn" type="submit"> 
                       <i class="las la-trash-alt"></i> Изтрий
                       </button>
                </div>
          </div>
        </div>
        </form>
      </div>
    </div>
    <!--End Login Form -->
   
</div>

